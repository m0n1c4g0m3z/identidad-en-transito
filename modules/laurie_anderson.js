// Sketch de OpenProcessing para Laurie Anderson

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
  textAlign(CENTER, CENTER);
  textSize(32);
  fill(255);
}

function draw() {
  background(0);
  text("Laurie Anderson", width / 2, height / 2);
}
