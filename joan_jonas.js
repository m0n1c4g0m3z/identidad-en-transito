// Sketch de OpenProcessing para Joan Jonas

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
  textAlign(CENTER, CENTER);
  textSize(32);
  fill(255);
}

function draw() {
  background(0);
  text("Joan Jonas", width / 2, height / 2);
}
