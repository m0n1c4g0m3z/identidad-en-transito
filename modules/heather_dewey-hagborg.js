// Sketch de OpenProcessing para Heather Dewey-Hagborg

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
  textAlign(CENTER, CENTER);
  textSize(32);
  fill(255);
}

function draw() {
  background(0);
  text("Heather Dewey-Hagborg", width / 2, height / 2);
}
