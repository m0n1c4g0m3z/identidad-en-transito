// Plantilla base para módulo de artista
// Sustituir 'Nombre' por el nombre real del artista

function initNombre() {
  // Inicializaciones específicas del módulo (carga de sonido, imágenes, etc.)
}

function drawNombre() {
  background(0);
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(24);
  text("Módulo de Nombre", width / 2, height / 2);
}
