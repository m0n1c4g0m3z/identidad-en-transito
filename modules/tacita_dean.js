// Sketch de OpenProcessing para Tacita Dean

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
  textAlign(CENTER, CENTER);
  textSize(32);
  fill(255);
}

function draw() {
  background(0);
  text("Tacita Dean", width / 2, height / 2);
}
